name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: |
            3.9
            3.10
            3.11
            3.12
            3.13
      - run: |
          for py in 3.9 3.10 3.11 3.12 3.13; do
            echo "::group::Python $py"
            python"$py" -m pip install -e ".[dev]"
            python"$py" -m pytest tests/unit tests/integration -v
            echo "::endgroup::"
          done
      - name: Lint
        run: |
          pip install -e ".[dev]"
          make lint
